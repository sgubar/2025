// dk_tool.h - заголовочный файл с объявлениями функций
// Этот файл содержит только объявления функций, которые будут использоваться в программе
#ifndef DK_TOOL_H
#define DK_TOOL_H

#include <stdbool.h>  // для использования типа bool (true/false)

// Функция проверяет корректность введенного числа
// Возвращает true, если строка корректна, false - если нет
bool validate_input(const char* str);

// Функция преобразует строку в число
// Принимает строку символов и возвращает целое число
int str_to_num(const char* str);

#endif // DK_TOOL_H
